name: Dependabot lockfile sync

# Dependabot bumps versions in Directory.Packages.props but cannot regenerate the
# packages.lock.json files (unsupported for Central Package Management), so its PRs fail
# CI's --locked-mode restore. This workflow regenerates the lockfiles on each Dependabot PR
# and pushes the result back onto the PR branch.
#
# To make the pushed commit re-trigger CI, store a PAT (contents: write) as the Dependabot
# secret DEPENDABOT_LOCKFILE_PAT. Without it the push still happens via GITHUB_TOKEN, but
# GITHUB_TOKEN pushes do not start new workflow runs, so CI must then be re-run manually.
on:
  pull_request:
    branches: ["main"]

permissions:
  contents: write

# Actions are pinned to commit SHAs (not mutable tags) so a retagged or compromised action
# version cannot silently enter the pipeline.
jobs:
  sync-lockfiles:
    if: github.actor == 'dependabot[bot]'
    runs-on: ubuntu-latest

    steps:
      # Check out the PR head branch (not the merge ref) so we can push back to it. Prefer
      # the PAT so the resulting commit re-triggers CI; fall back to the GITHUB_TOKEN.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.head_ref }}
          token: ${{ secrets.DEPENDABOT_LOCKFILE_PAT || secrets.GITHUB_TOKEN }}

      # Use the exact SDK pinned in global.json so the implicit Blazor asset package
      # (Microsoft.AspNetCore.App.Internal.Assets) matches the lockfiles CI restores.
      - uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          global-json-file: global.json

      # --force-evaluate rewrites the lockfiles to match the bumped versions. The Aspire
      # AppHost has no lockfile (RestorePackagesWithLockFile=false) and is unaffected.
      - name: Regenerate lockfiles
        run: dotnet restore --force-evaluate

      # No-op (and no loop) when the lockfiles already match: a PAT push re-runs this
      # workflow, the second pass finds them in sync and pushes nothing.
      - name: Commit and push if lockfiles changed
        run: |
          if git diff --quiet -- '**/packages.lock.json'; then
            echo "Lockfiles already in sync; nothing to do."
            exit 0
          fi
          git config user.name "dependabot[bot]"
          git config user.email "49699333+dependabot[bot]@users.noreply.github.com"
          git add '**/packages.lock.json'
          git commit -m "chore(deps): regenerate packages.lock.json"
          git push
